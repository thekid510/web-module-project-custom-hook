import { useLocalStorage } from './useLocalStorage'

export const UseDarkMode = (key)  => {
const [someValue, setSomeValue] = useLocalStorage("toggled")                           
}
return [storedValue, setValue];
    